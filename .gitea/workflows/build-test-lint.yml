name: lint-build-test
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  lint-build-test:
    runs-on: ubuntu-latest
    container: 
      image: docker.ascendise.ch/ascendise/cmake-runner:latest
      credentials:
        username: ${{ secrets.DOCKER_USER }}
        password: ${{ secrets.DOCKER_PASS }}
    steps:
      - name: Checkout project
        uses: actions/checkout@v3
        with:
          submodules: 'true'
      - name: Cache build output
        id: build-cache 
        uses: actions/cache/restore@v4
        with:
          path: ./build
          key: build-output
      - name: Install build dependencies
        run: apt-get -y install python3-jinja2
      - name: Build and Test
        uses: lukka/run-cmake@v10.7
        with: 
          configurePreset: dev-vcpkg-ninja
          buildPreset: dev-vcpkg-debug
          testPreset: dev-vcpkg-test
      - name: Cache build output
        id: build-cache 
        uses: actions/cache/save@v4
        with:
          path: ./build
          key: build-output
      - name: Linter
        working-directory: ./build/dev-vcpkg-ninja
        run: run-clang-tidy
